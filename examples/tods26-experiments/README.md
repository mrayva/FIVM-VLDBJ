# TODS26 TPCH Q10 Experiments

This directory holds the Q10 experiment workflow implementation: scripts, generated assets, logs, and plots.

The `docs/` references from the original workflow are not present in this repository snapshot. Use the checked-in scripts here directly:
- `run_q10_matrix.sh` and `run_q10_count_matrix.sh` for the main matrix runs
- `plots/run_plots.sh` and `plots/run_round_plots.sh` for plot generation
- `queries/` for generated SQL assets and variable orders
